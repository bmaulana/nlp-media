Set up conda environment: Follow instructions in 'ENV SETUP INSTRUCTIONS.txt' to set up a conda environment and install required dependencies (e.g. SpaCy, TensorFlow).

Activate conda environment: Run 'activate <env-name>' in cmd/sh e.g 'activate nlp-media'.

Run script: 'python pipeline.py' in cmd/sh while within the conda environment. 